## 2025-04-03 - Added ARIA Label to Primary Search Input
**Learning:** For aesthetic minimalism, the app's design system omits explicit visual `<label>` elements on primary search inputs.
**Action:** Always use `aria-label` attributes on these elements to ensure screen reader accessibility.